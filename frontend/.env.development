# Leave empty so the dev server proxies API calls to http://localhost:8000.
# Start backend first: cd backend && python -m uvicorn main:app --reload
# (Only set REACT_APP_API_BASE for production build or when testing against another host.)
REACT_APP_API_BASE=
